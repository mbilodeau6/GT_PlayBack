{
  "routes": [
    {
      "route": "/api/Games/*",
      "rewrite": "/api/proxy"
    },
    {
      "route": "/api/Games",
      "rewrite": "/api/proxy"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/css/*", "/js/*", "/replay/*", "/api/*", "/*.ico", "/*.png", "/*.svg"]
  }
}
